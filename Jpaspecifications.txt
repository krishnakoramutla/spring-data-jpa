Spring Data JPA - Using Specifications to execute JPA Criteria Queries. Spring Data provides Specification interface which can be used to execute JPA criteria queries. As seen above there's only one abstract method 'toPredicate()' which returns javax. persistence
